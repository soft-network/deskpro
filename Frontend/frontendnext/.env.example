# =============================================================================
# Frontend — Environment Variables
# Copy this file to .env.local and adjust if needed.
# Never commit .env.local — it is gitignored.
# =============================================================================

# URL used by client-side (browser) components to reach the backend API.
# For local dev: http://localhost:8000/api
# For Docker:    http://localhost:8000/api  (browser always goes through mapped port)
NEXT_PUBLIC_API_URL=http://localhost:8000/api
